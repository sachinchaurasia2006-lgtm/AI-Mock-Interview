spring:
  datasource:
    url: jdbc:h2:mem:mockmate-test;MODE=MySQL;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate.ddl-auto: create-drop

app:
  jwt.secret: test-only-secret-key-that-is-long-enough-for-hmac-sha
  cors:
    allowed-origins: http://localhost:5173
  password-reset:
    expose-development-token: true
